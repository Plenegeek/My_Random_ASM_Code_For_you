==============================================
Any ASM code
==============================================

So, like just use like NASM, i think that only NASM and another compiler uses the " ; " commment thing, so just use that, its not that hard ig...
